---
title: typescript-as-data
description: TypeScript in place of YAML. A file that is data is typed JSON, and it means the same thing whether it is folded or run.
template: splash
hero:
  tagline: TypeScript in place of YAML. Typed, checked at the keystroke, and turned into the artifact without running.
  actions:
    - text: Try it in ten minutes
      link: /typescript-as-data/try-it/
      icon: right-arrow
    - text: Add it to your platform
      link: /typescript-as-data/for-your-platform/
      icon: rocket
      variant: minimal
    - text: The specification
      link: /typescript-as-data/introduction/what-this-is/
      icon: document
      variant: minimal
---

- Pure synthesis, no execution. The file becomes the artifact without being run. [How](/typescript-as-data/what-it-enables/no-execution/)
- Semantic linting. The same check at the keystroke and in CI, over real values, across resources. [How](/typescript-as-data/what-it-enables/semantic-linting/)
- Round-trip generation. Live state or an existing artifact becomes source that folds back to what it came from. [How](/typescript-as-data/what-it-enables/round-trip/)

## The same policy twice

```yaml title="governance.yml"
orgs:
  my-org:
    repos:
      api:
        hasWiki: false
        allowSquashMerge: true
        topics: [service, api]
        branchProtection:
          - ruleName: main
            requiredApprovals: 1
            enableStatusCheck: true
            statusCheckContexts: [ci]
```

```ts title="governance.ts"
import type { GovernanceConfig } from "@intentius/forgejo-warden";
import { service } from "./helpers.ts";

export const policy = {
  orgs: {
    "my-org": {
      repos: { api: service("api"), web: service("web") },
    },
  },
} satisfies GovernanceConfig;
```

The second one has completion and a type error on a misspelt key. It reuses one helper across repositories. The tool reads the same object from either file and never runs the second one to get it.
